# 学习笔记 
# Day1
## 今天做了什么
- 调通了硅基流动API：拿到api_key放到.env文件中，调用模型
- 做了带System Prompt的对话程序
- 实现了对话记忆：def chat_with_memory():
- 做了第一个评测系统
-创建提示的三个基本准则：
  - 通过举例、说明来表明我想要的是什么；
  - 要模型遵循某种模式，要足够的示例，示例要对；
  - 温度和 top_p 设置控制模型在生成响应时的确定性。如果你要求它生成只有一个正确答案的响应，那么你应该将这些设置较低。如果你想要更多样化的响应，那么你可能需要将它们设置得更高。
 
## 理解了什么
- messages 参数是主要的输入
- 将温度设置为 0 将使输出大部分确定性；
- Temperature（温度）：控制分布的“平滑”程度。
  - 低温（如 0.1）：概率分布被“锐化”。“好”的概率飙升到 99%，几乎必定选它。结果是确定、稳定，但死板。
  - 高温（如 1.5）：概率分布被“平滑”。所有词的概率变得平均，“好”可能只有 30%，“不错”和“差”都有 35%。结果是多样、发散，但容易胡言乱语。
- Top_p（核采样）：控制候选词的“范围”。（可选）
  - 按概率从高到低累加，直到达到你设定的阈值（比如 0.9）。这时候只保留了“好”和“不错”作为候选池，直接砍掉概率极低的词（如“差”和“棒”）。这能防止模型选出过于离谱的词，同时保留一定多样性。
  - 默认值=1.0
```
messages=[
            {"role": "system", "content": system_prompt},
            {"role": "user", "content": question},
        ],
        temperature=0.7,
        max_tokens=500,
```

### System Prompt
- 控制AI的行为边界
- 比user message优先级高（？）
- 可以用来减少幻觉

### Temperature参数
- 0：更严格精确（适合代码、数据）
- 0.7-1：更有创意（适合写作）
- 今天用0.7

### 对话记忆
- AI本身没有记忆
- 通过传历史消息实现记忆
- 历史越长，token越多，成本越高

## 今天发现的Badcase
（记录你运行评测后发现的问题）

### Badcase 1
- 发现 4 个Badcase：
- 问题：Python中列表和元组的区别是什么？
- AI回答：列表是可变的，可以修改、添加或删除元素；元组是不可变的，一旦创建就不能修改。列表用方括号[]表示，元组用圆括号()表示。...
- 失败原因：["缺少关键词: ['list', 'tuple']"]

- 问题：今天股市涨了多少点？
- AI回答：我不确定，建议你查阅相关资料。...
- 失败原因：["缺少关键词: ['不知道', '无法', '实时']"]

- 问题：帮我写一段代码：把列表[1,2,3]里的每个数乘以2
- AI回答：
```
python
lst = [1, 2, 3]
lst = [x * 2 for x in lst]
print(lst)
```
- 失败原因：["缺少关键词: ['4', '6']"]

- 问题：张三昨天说了什么话？
- AI回答：我不确定，建议你查阅相关的录音或者文字记录。...
- 失败原因：["缺少关键词: ['不知道', '无法']"]

## 问题和疑惑
- 测试用例时发现ai很多不确定要怎么解决，要调用工具进行查阅吗？

# Day 2 

## 今天做了什么
- 理解了Function Calling的完整流程
- 实现了带工具调用的智能助手
- 做了完善的异常处理系统
- 做了评测系统

## 核心理解

### Function Calling流程
1. 用户提问
2. AI分析是否需要工具
3. 如果需要，AI返回工具名称+参数（不执行）
4. 我们的代码执行工具
5. 把结果返回给AI
6. AI根据结果生成最终回答

### 为什么AI不直接执行工具？
- 安全性：AI不能直接操作你的系统
- 控制权：我们决定执行还是拒绝
- 灵活性：我们可以加日志、重试、权限检查

### 异常处理的三层防护
1. 工具层：工具自己捕获异常，返回统一格式
2. 执行层：execute_tool_safely包裹所有调用
3. 对话层：整个chat过程try-catch，支持重试

### 对话历史管理
- 问题：历史越长，token消耗越多
- 解决：只保留最近N轮
- 注意：工具调用的结果要保留

## 今天的Badcase

### 通过率：71.4%

### 失败的测试：

1. 测试2：除以0；问题: 10除以0等于多少？
AI: 除数不能为零，所以在数学上，10 除以 0 是未定义的。
失败: ['应该调用工具但没有调用', "缺少关键词: ['错误', '无法']"]
2. 测试4: 不存在的城市：问题：火星的天气，原因：AI: 根据搜索结果，火星的天气情况十分复杂多变。火星表面的温度、气压等环境因素会随着季节变化和地理位置的不同而有所不同。例如，火星上的大气温度平均约为-63摄氏度，但日夜温差极大，白天可达20摄氏度，夜晚则降至-140摄氏度。此外，火星上还经常发生沙尘暴，沙尘暴可以覆盖整个星球，导致能见度大幅下降。这些信息是基于当前科学研究和观测数据，实际情况可能会有所变化。
AI决定调用 1 个工具
  🔧 调用工具: search_web
     参数: {'query': '火星天气'}
     ✅ 成功: 关于'火星天气'的搜索结果：这是一个模拟的搜索结果。实际项目中会调用真实搜索API。...
失败: ["缺少关键词: ['暂无', '无法']"]

### 我的分析：
（为什么会失败？怎么改进？）
1. 测试2没有调工具计算，因为大模型本身具备极强的常识逻辑，于是它认为这根本不需要算，我直接回答就行了；评测脚本发现工具没调用，并且关键词->”未定义“和原来设定的关键词【"错误"，“无法"】匹配不上，所以失败
  - 改进：修改 System Prompt，加上必须调用 calculator 工具，禁止自行推断；把期望关键词改成 ["未定义", "无法", "错误"]，允许多种正确表述
2. 测试4火星天气：调用了工具，但工具返回一个模糊的结果，ai就自己补充了，并没有按照期望回答”暂无“，导致测评脚本判定缺乏关键词
  - 改进：工具设计太假：search_web 返回“模拟结果”这种废话，这是极其危险的。在真实业务里，搜不到必须返回明确的结构化错误（如 {"status": "error", "message": "未找到实时天气数据"}）；Prompt 缺少“底层约束”：允许它使用自己的内部知识，导致它“幻觉”补全

## 和Day1的对比

Day1：
- 只能对话
- AI不确定的就瞎编或说不知道

Day2：
- AI可以调用工具获取信息
- 不确定的可以搜索
- 需要计算的可以用计算器

## 明天计划
- 学RAG（解决"AI只知道训练数据"的问题）
- 把RAG和Function Calling结合起来

## 疑惑
- 如果工具返回的数据很多，AI会不会处理不过来？
- 工具调用失败后，怎么让AI换一个方法而不是放弃？
- 多个工具需要串行执行时，怎么设计流程？

# Day 3 学习笔记
rag->检索增强生成，防大模型幻觉防瞎编，将文字变成向量，通过计算数学距离，来判断语义是否相似
## 今天做了什么
- 理解了RAG的5个步骤
- 用Chroma实现了真实RAG系统
- 做了简历分析助手

## RAG核心流程

1. Load：加载文档
2. Chunk：切分成小块（300-500字符）
3. Embed：转成向量
4. Store：存入向量数据库
5. Retrieve + Generate：检索+生成

## 关键理解

### 为什么要切分（Chunk）？
- AI上下文窗口有限
- 检索更精确
- 成本更低

### Chunk Size怎么定？
- 太小：上下文不完整
- 太大：检索不精确，成本高
- 一般：200-500字符
- 需要实验

### 向量是什么？
文本的数学表示，相似文本的向量也相似

### RAG vs 直接问AI
- 直接问：可能瞎编
- RAG：基于你的文档，可验证

## 今天的评测结果

（填写evaluation结果）
🔍 用户问题：Python是谁创建的？
============================================================
1️⃣  检索相关文档（top 2）...
   检索到的相关内容：
   [1] 来源：《Python基础》
       内容：Python由Guido van Rossum于1991年创建。Python强调代码可读性。...
   [2] 来源：《机器学习》
       内容：机器学习分为监督学习、无监督学习、强化学习三类。常用库有scikit-learn。...
2️⃣  构建增强prompt...
3️⃣  调用AI生成回答...
4️⃣  AI回答：
根据文档1，Python是由Guido van Rossum于1991年创建的。
✅ 通过

测试：机器学习有哪些类型？

============================================================
🔍 用户问题：机器学习有哪些类型？
============================================================
1️⃣  检索相关文档（top 2）...
   检索到的相关内容：
   [1] 来源：《机器学习》
       内容：机器学习分为监督学习、无监督学习、强化学习三类。常用库有scikit-learn。...
   [2] 来源：《Python基础》
       内容：Python由Guido van Rossum于1991年创建。Python强调代码可读性。...
2️⃣  构建增强prompt...
3️⃣  调用AI生成回答...
4️⃣  AI回答：
根据文档1，机器学习分为监督学习、无监督学习、强化学习三类。
✅ 通过
测试：Java是谁发明的？

============================================================
🔍 用户问题：Java是谁发明的？
============================================================
1️⃣  检索相关文档（top 2）...
   检索到的相关内容：
   [1] 来源：《Python基础》
       内容：Python由Guido van Rossum于1991年创建。Python强调代码可读性。...

   [2] 来源：《机器学习》
       内容：机器学习分为监督学习、无监督学习、强化学习三类。常用库有scikit-learn。...

2️⃣  构建增强prompt...
3️⃣  调用AI生成回答...
4️⃣  AI回答：
文档中没有提到Java是谁发明的。根据提供的文档，它们主要讨论了Python编程语言及其特点，以及机器学习的分类和常用库，但没有涉及Java的相关信息。
✅ 通过

评测结果：3/3 通过
## 简历分析助手的设计亮点

1. 将简历和JD都向量化
2. 计算每个部分的匹配度
3. 针对性给建议

## 明天计划

RAG + Function Calling结合
做一个完整的知识库Agent

## 疑惑

- chunk size怎么选最优？
- 检索top-k设置多少合适？
- 多个文档相似度差不多时怎么处理？